/// Window Functions

// HTML focus
if (mousep(mb_any)) focus_window();

// Keep mouse within game window
//window_mouse_set(clamp(window_mouse_get_x(), 2, window_get_width() - 2), clamp(window_mouse_get_y(), 2, window_get_height() - 2));